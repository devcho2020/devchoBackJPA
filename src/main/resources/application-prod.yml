spring:
  application:
    name: devcho-back-jpa
  datasource:
    url: jdbc:mariadb://localhost:3306/dev_db
    username: root
    password: dev_password
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
